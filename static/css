Dark Modern Theme:

Background: #1a1f2c (dark blue)

Containers: #2d3748 (darker gray-blue) with rounded corners

Accent Color: #4299e1 (vibrant blue for buttons/highlights)

Interactive Elements:

Buttons:

Blue fill with white text

Smooth hover effects (transform: translateY(-3px), shadow increase)

Press effect (:active state)

Table Rows:

Hover highlights (background-color: #353f53)

Bottom borders (#4299e1)

Layout & Typography:

Centered containers with max-width: 80%

Font: Segoe UI (clean, readable)

Text: White with blue headings (#4299e1)

Special Components:

Video Feed Container: Clean frame for live webcam

Instructions Box: Left-border accent with hover animation

Forms:

Input fields with light background (#edf2f7)

File upload styling

Responsive: Flexbox/CSS Grid for adaptability

Total Size: ~150 lines (efficient, no bloat)
